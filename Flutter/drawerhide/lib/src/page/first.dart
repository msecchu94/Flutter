import 'package:flutter/material.dart';



class FirtPage extends StatelessWidget {
  const FirtPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child:Text('First Page')),
    );
  }
}